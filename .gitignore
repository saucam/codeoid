node_modules
node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.*

# Runtime logs from `nohup bun src/cli.ts start > .run/daemon.log` etc.
.run/

# coverage / test-result artifacts
coverage/
junit.xml

# codeoid is a bun project — bun.lock is the lockfile; never commit an npm lock
package-lock.json
